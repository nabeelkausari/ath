import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  chip: {
    padding: '0.39vh 1.04vh',
    fontSize: '1.43vh',
    borderRadius: '1.3vh',
    textTransform: 'capitalize',
    display: 'table-cell',
    lineHeight: 1,
  },
}));

export default useStyles;
